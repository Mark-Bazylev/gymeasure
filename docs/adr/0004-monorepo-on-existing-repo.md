# Monorepo replacing the Expo-only scaffold

The existing Mark-Bazylev/gymeasure GitHub repo held only a create-expo-app scaffold. We replaced it with a monorepo (`apps/mobile`, `apps/api`, `packages/shared`) under the same remote so mobile and API share types and one deployable product identity without splitting repos.
