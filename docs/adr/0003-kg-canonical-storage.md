# Kilograms are canonical; display unit is a preference

All Set weights are stored in kilograms. The UI may show pounds via a Settings preference by converting at the edges. Volume math and Compare series always use kg so buddy charts stay consistent regardless of display preference.
